{
  "name": "live-interpreter",
  "productName": "Live Interpreter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "electron/main.mjs",
  "description": "Native macOS meeting interpreter with app-level audio capture and independent output routing",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check electron/main.mjs && node --check electron/preload.cjs && node --check renderer/app.js && node --check renderer/captions.js && node --check renderer/settings.js && node --check shared/ui-i18n.mjs",
    "package:mac": "node scripts/package-mac.mjs"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.2",
    "electron": "^43.1.1"
  }
}
