import { FormEvent, useState } from "react";import { api } from "./api";
export function ResetPassword({token}:{token:string}){const[password,setPassword]=useState('');const[message,setMessage]=useState('');async function submit(e:FormEvent){e.preventDefault();try{await api('auth/reset-password.php',{method:'POST',body:JSON.stringify({token,password})});setMessage('パスワードを変更しました。トップページからログインしてください。')}catch(error){setMessage(error instanceof Error?error.message:'変更できませんでした。')}}return <main className="standalone-page"><form className="auth-dialog reset-card" onSubmit={submit}><p className="section-kicker">ACCOUNT SECURITY</p><h2>パスワード再設定</h2><label>新しいパスワード<input type="password" minLength={12} value={password} onChange={e=>setPassword(e.target.value)} required autoFocus/></label><button className="primary auth-submit">パスワードを変更</button>{message&&<p className="form-message">{message}</p>}</form></main>}
